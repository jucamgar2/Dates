---
const paragraphs = [
  "Como ya sabes falta poco para nuestro primer aniversario...",
  "Y me preguntaba... ¿Tienes ganas de saber tu regalo?",
  "Pues esto no te va a ayudar",
  "¿O si?",
  "Bueno, primero",
  "¿Me dices tus preferencias para que pueda organizar la cita?"
];
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>Recuerdo de nuestras citas</title>
	</head>
	<body>
        <div id="popup" class="popup">
            <div class="popup-content">
                <p id="text"></p>
                <button id="yesButton">Sí</button>
                <button id="noButton">No</button>
            </div>
        </div>

    </body>
</html>

<style>
    html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		font-family: 'Franklin Gothic Medium', 'Arial Narrow', Arial, sans-serif;
		background-image: url('/background.svg');
		background-size: cover;
		background-repeat: repeat;
		color: #462d34;
		
	}

    .popup {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-color: rgba(0, 0, 0, 0.5);
        display: flex;
        justify-content: center;
        align-items: center;
        visibility: hidden;
        opacity: 0;
        transition: visibility 0s, opacity 0.3s;
    }

    .popup-content {
        position: absolute;
        background: white;
        padding: 20px;
        border-radius: 8px;
        text-align: center;
        box-shadow: 0 4px 8px rgba(0, 0, 0, 0.2);
        height: 150px;
        width: 200px;
    }

    .popup-content button {
        margin: 10px;
        padding: 10px 20px;
        font-size: 16px;
        cursor: pointer;
        border: none;
        border-radius: 4px;
    }

    .popup-content button#yesButton {
        background-color: #4CAF50;
        color: white;
    }

    .popup-content button#noButton {
        background-color: #f44336;
        color: white;
    }
</style>

<script>
  window.addEventListener('load', () => {
    const popup = document.getElementById('popup');
    popup.style.visibility = 'visible';
    popup.style.opacity = '1';
    const screenWidth = window.innerWidth;
    const screenHeight = window.innerHeight;

    const yesButton = document.getElementById('yesButton');
    const noButton = document.getElementById('noButton');

    // 🔹 Ocultamos botones al inicio
    yesButton.style.display = "none";
    noButton.style.display = "none";

    yesButton.addEventListener('click', () => {
      window.location.href = '/days/Nov124'; 
    });

    const popupContent = document.querySelector('.popup-content');
    noButton.addEventListener('click', () => {
      popupContent.style.left = Math.random() * (screenWidth -200) + 'px';
      popupContent.style.top = Math.random() * (screenHeight -200) + 'px';
    });

    const paragraphs = [
      "Bueno pues oficialmente hoy es nuestro aniversario",
      "Yo creo que ya va siendo hora de que sepas cual va a ser tu regalo",
      "¿Te ayudara esta página a saber tu regalo?",
      "¿Quien sabe?",
      "¿Estas lista?",
    ];

    let index = 0;
    const textElement = document.getElementById("text");

    function updateText() {
      if (index < paragraphs.length) {
        textElement.textContent = paragraphs[index];

        // 🔹 Si es la última frase, mostramos los botones
        if (index === paragraphs.length - 1) {
          yesButton.style.display = "inline-block";
          noButton.style.display = "inline-block";
        }

        index++;
        setTimeout(updateText, 5000);
      }
    }

    updateText();
  });
</script>